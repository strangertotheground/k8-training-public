apiVersion: v1
kind: Service
metadata:
  name: helloworld-service
spec:
  containers:
    ports:
    - port: 310001
      targetPort: nodejs-port
      protocol: TCP
    selector:
      app: helloworld
    type: LoadBalancer  
